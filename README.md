
# Medication Reminder Backend

## Setup
1. Install dependencies:
pip install -r requirements.txt

2. Run the server:
uvicorn main:app --reload

3. Use API via `/docs` (Swagger UI).

## Features
- User Authentication (Caregiver)
- Medication & Individual Management
- Twilio Call Reminders with Retry Logic
